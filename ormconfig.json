{
  "type": "postgres",
  "host": "${DB_HOST}",
  "port": "${DB_PORT}",
  "username": "${DB_USERNAME}",
  "password": "${DB_PASSWORD}",
  "database": "${DB_DATABASE}",
  "entities": ["src/models/**/*.{ts,js}", "dist/models/**/*.{ts,js}"],
  "synchronize": true,
  "logging": true
}
